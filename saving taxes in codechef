#include <stdio.h>
int main() {
    int T;
    scanf("%d", &T);      
    while (T--) {
        int X, Y;
        scanf("%d %d", &X, &Y);
        int investment = X - Y;
        printf("%d\n", investment);
    }    
    return 0;
}
